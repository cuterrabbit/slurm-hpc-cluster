[defaults]
inventory = inventory/hosts.ini
host_key_checking = False
retry_files_enabled = False
interpreter_python = auto_silent
# vault 비밀번호 경로는 환경마다 다르므로(헤드 로컬 vs CI) 여기 하드코딩하지 않는다.
# 헤드에서는 ANSIBLE_VAULT_PASSWORD_FILE 환경변수로, CI에서는 워크플로가 명시적으로 주입한다.
