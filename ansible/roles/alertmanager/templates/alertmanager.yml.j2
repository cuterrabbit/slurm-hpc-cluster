# {{ ansible_managed }}
# 실제 알림 채널(이메일/Slack 등)은 후속 진행 
# 여기서는 알림을 쏘는지만 Alertmanager UI/API로 확인한다. receiver는 자리만 채워둠.

route:
  receiver: default
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 1h

receivers:
  - name: default
